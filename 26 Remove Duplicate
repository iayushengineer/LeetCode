class Solution {
    public int removeDuplicates(int[] nums) {
        // int k =0;
        // int num = nums[k];
        // for(int i = 1; i<nums.length; i++){

        //      if(num != nums[i]){

        //         nums[++k] = nums[i];
        //         num = nums[i];
            
        //     }

        //     }
        //     return ++k;
        

        int i = 0 ;

        for(int j =1 ; j<nums.length;j++){
            if(nums[j]!=nums[i]){
                
                nums[++i]=nums[j];
            }
        }
        return ++i;
    }
}
